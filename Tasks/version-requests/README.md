# Version requests

Each completed medium Task/Bug/correction or verified Epic/release transition adds one immutable `.json` request in this directory. Use a collision-resistant filename such as `<UTC>-<work-item>-<UUID>.json`. Never edit or delete an accepted request. The GitHub allocator validates and processes requests exactly once by `request_id`; see `Tasks/tasks_file_structure_v4.md`.
